# Guardrail: FFI boundary & ABI stability

**When to consult:** the C ABI surface (`core-ffi/src/lib.rs`), the cbindgen config
(`core-ffi/cbindgen.toml`), the generated header (`core-ffi/include/safetystrip.h`),
the config serialization (`core/src/config.rs`), or the capabilities/version query
(`CAPABILITIES_JSON` in `core/src/lib.rs`).

The C ABI is the **frozen, language-neutral** contract between the core and every
shell. A future Windows or Linux shell — in any language — must be able to consume
it unchanged. So the surface is kept tiny, data-driven, and versioned, and any real
change to it is a deliberate compatibility event.

## The surface (all of it)

| Symbol | Contract |
|---|---|
| `ss_abi_version() -> u32` | Integer ABI version for capability negotiation. Currently `SS_ABI_VERSION = 1`. |
| `ss_capabilities_json() -> *const c_char` | Pointer to a **static**, NUL-terminated JSON self-description (name, version, config-schema version, supported operations). Valid for the process lifetime; **must not be freed**; never null. |
| `ss_transform(input, input_len, config_json, out, out_len) -> SsStatus` | Transform `input` per `config_json`. On `Ok`, `*out`/`*out_len` is a heap buffer the caller must free. On any error, `*out`=null/`*out_len`=0. |
| `ss_buffer_free(ptr, len)` | Free (and zeroize) a buffer from `ss_transform`. Null `ptr` is a no-op. Free at most once with the exact matching `(ptr, len)`. |
| `SsStatus` (`repr(C)` enum) | `Ok=0`, `ErrNullArg=1`, `ErrInvalidConfig=2`, `ErrInternal=3`. No global error state. |

Memory/ownership/error contract (do not change one side only): input is UTF-8,
**lossy-decoded** (invalid bytes → U+FFFD) so adversarial bytes never make it fail;
`input` may be null only if `input_len == 0`; out-params must be writable and are
initialized on every return path; the output buffer is a leaked `Box<[u8]>`
reclaimed (and zeroized) by `ss_buffer_free`; a panic in the core is caught and
returned as `ErrInternal` rather than unwinding across FFI.

## The rules

1. **Adding or changing a *transform* must NOT change the ABI.** Feature selection
   crosses as the `config_json` string. A new operation is config *data* — a new
   `Operation` enum variant, a pipeline arm, and a `CAPABILITIES_JSON` entry. None of
   that touches the four C symbols. If your transform work edits `core-ffi/src/lib.rs`
   signatures, you are doing it wrong.
2. **The checked-in header is the source of truth.** `core-ffi/include/safetystrip.h`
   is generated by cbindgen and committed. Never hand-edit it. After an intended ABI
   change, regenerate it with `cargo xtask gen-header`. The macOS shell's
   `CSafetyStrip` module re-includes this exact file (it does not copy it), so there
   is one header, not two.
3. **A real ABI change is a compatibility event.** If you change a signature, a
   struct/enum layout, or the memory-ownership contract, you must:
   - bump `SS_ABI_VERSION` in `core-ffi/src/lib.rs`,
   - run `cargo xtask gen-header` to regenerate the header,
   - **call it out in the PR**, and confirm a non-Swift shell could still consume the
     boundary.
4. **Keep the surface narrow.** Resist adding new entry points. The four symbols
   above are sufficient because behavior is data. New capability should almost always
   be a new operation, queried via `ss_capabilities_json`, not a new function.
5. **Keep config back-compatible or bump `CONFIG_VERSION`.** The config schema is
   versioned independently (`core/src/config.rs`, currently `1`). An incompatible
   schema change bumps `CONFIG_VERSION`; `parse_config` rejects mismatched versions
   so a shell detects it deterministically. `CAPABILITIES_JSON`'s `config_version`
   must stay equal to `CONFIG_VERSION` (a unit test enforces this).
6. **No `unsafe` outside this crate, and follow the FFI safety rules** in
   [memory-safety](memory-safety.md) (validate pointers, `catch_unwind`, zeroize).

> Note: `CAPABILITIES_JSON` advertises `change_case` with a descriptor field
> `"cases":[...]` listing the *allowed* values; an actual config instance uses the
> singular field `"case"` (e.g. `{"op":"change_case","case":"title"}`). The
> capabilities JSON is a menu, not an instance — keep them consistent when you add
> cases/params.

## Enforcing checks

- `cargo xtask check-abi` — fails CI if the generated header differs from the
  checked-in one (the drift detector). Fast inner loop: run it on its own.
- `cargo test -p safetystrip-ffi` — ABI round-trip, null-arg handling, ownership.
- `cargo test -p safetystrip-core` — config serde round-trip + version tests, and
  the `CAPABILITIES_JSON`/`CONFIG_VERSION` consistency test.

## What a PR must call out

- **Whether the ABI changed.** If yes: the `SS_ABI_VERSION` bump, the regenerated
  header, and an explicit note that a non-Swift shell can still consume it. If no:
  state that the change is config *data* only and the ABI is unchanged.
- Any `CONFIG_VERSION` bump and why (what became incompatible).
- New operations added to `CAPABILITIES_JSON`.
