/* SwiftPM requires at least one compilable source in a C target. The actual
 * symbols are provided by the prebuilt Rust staticlib (libxpare_ffi.a),
 * linked via XPareCore's linkerSettings. This translation unit only
 * exists so the target has something to build. */
