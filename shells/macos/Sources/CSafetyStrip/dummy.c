/* SwiftPM requires at least one compilable source in a C target. The actual
 * symbols are provided by the prebuilt Rust staticlib (libsafetystrip_ffi.a),
 * linked via SafetyStripCore's linkerSettings. This translation unit only
 * exists so the target has something to build. */
